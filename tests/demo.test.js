describe('pruebas en <DemoComponent />', () => { 
    test("esta prueba no debe fallar message", () => {
        // Inicializacion
        // Estimulo
        // Observar el comportamiento
    
        const message1 = 'Hola mundo';
    
        const message2 = message1.trim();
    
        expect(message1).toBe(message2);
     
    })
 })

